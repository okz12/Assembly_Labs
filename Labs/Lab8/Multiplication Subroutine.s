	AREA	ArmSubrEx, CODE, READWRITE
	ENTRY
		
		LDR R13, =STACKL + 36
		MOV R1, #5
		MOV R2, #3
		BL MULT
		ADD R3, R0, #1
STOP	B	STOP

MULT	STMED R13!, {R1, R14}
		MOV	R0, #0
		CMP	R1, #0
		BEQ	MULTEND
MULOOP	ADD R0, R0, R2
		SUBS	R1, R1, #1
		BNE MULOOP
MULTEND	LDMED	R13!, {R1, PC}
STACKL	FILL 40
	END